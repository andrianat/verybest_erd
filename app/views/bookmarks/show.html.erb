<div class="page-header">
  <h3>Bookmark #<%= @bookmark.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Venue</dt>
      <dd>
        <% if @bookmark.user.present? %>
          <a href="/venues/<%= @bookmark.venue_id %>">
            <%= @bookmark.user.name %>
          </a>
        <% end %>
      </dd>

      <dt>Dishes</dt>
      <dd>
        <% if @bookmark.dishes.present? %>
          <a href="/dishes/<%= @bookmark.dishes_id %>">
            <%= @bookmark.dishes.namedish %>
          </a>
        <% end %>
      </dd>

      <dt>Users</dt>
      <dd>
        <% if @bookmark.users.present? %>
          <a href="/users/<%= @bookmark.users_id %>">
            <%= @bookmark.users.created_at %>
          </a>
        <% end %>
      </dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/bookmarks" class="btn btn-primary">
        Back
      </a>
      <% if current_user == @bookmark.users %>
        <a href="/bookmarks/<%= @bookmark.id %>/edit" class="btn btn-warning">
          Edit
        </a>
        <a href="/delete_bookmark/<%= @bookmark.id %>" class="btn btn-danger" rel="nofollow">
          Delete
        </a>
      <% end %>
    </div>
  </div>
</div>
